# Active V2 Implementation Branch

A implementação visual V2 **não deve acontecer nesta branch de documentação**.

Branch ativa da Issue #5:

`codex/5-v2-clean-room`

Antes de implementar:

```bash
git fetch origin --prune
git switch codex/5-v2-clean-room
git pull --ff-only origin codex/5-v2-clean-room
```

A branch ativa contém o baseline executável, o clean-room `src/v2/`, a fronteira automática contra imports visuais legados e o fluxo visual-first.

Esta branch continua servindo como arquivo histórico/documental. Não criar Home V2 aqui.

import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: 'br.com.assistentepedagogico.app',
  appName: 'Assistente Pedagógico',
  webDir: 'dist',
  server: { androidScheme: 'https' },
};
export default config;

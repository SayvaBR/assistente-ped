# Codex Task — Accelerated Visual Mode

Read only these first:

1. `docs/WORKING_CONTEXT.md`
2. `docs/PRODUCTION_SPEED_PROTOCOL.md`
3. `docs/SCREEN_SPEC_ONBOARDING_ENTRY_V2.md`
4. current code directly involved in Splash/Onboarding/V2 routing

Open other documents only if the task actually touches that domain.

## Immediate objective

The current first screen after Splash is visually rejected as a final direction. Rebuild the **Onboarding Entry V2** so it strongly expresses the product identity, while also creating the minimum UI Lab needed to iterate faster.

Do not preserve the current composition merely because it exists.

## Parallel lanes

Use independent agents/worktrees when the Codex environment supports it and files do not conflict.

### Lane A — UI Compose (highest priority)

- implement/rebuild Onboarding Entry in `src/v2/`;
- use `docs/SCREEN_SPEC_ONBOARDING_ENTRY_V2.md` as the specific authority;
- treat the current screenshot as baseline/anti-target;
- replace the isolated generic icon + giant title + detached bottom card composition;
- create a tactile educational hero using mini product/workspace objects related to teacher routine;
- keep the primary CTA visible in the first viewport;
- first render at 390 px immediately;
- compare, correct the 3–5 biggest perceptual problems, render again;
- do not connect every edge case before the composition is convincing.

### Lane B — UI Lab MVP

Implement only the smallest useful version of `docs/UI_LAB_SPEC.md`:

- dev-only `/__lab` route or equivalent;
- direct preview for Onboarding Entry V2;
- 360 / 390 / 430 viewport controls;
- at least default + one secondary state;
- synthetic fixtures only;
- HMR must work;
- add one direct screenshot command/script if practical.

Do not turn the Lab into a large framework. Its job is to shorten the loop.

### Lane C — Targeted QA

- run `pnpm run check:fast` during iteration;
- use targeted unit/E2E only if behavior changed;
- no full Android pipeline for CSS/composition loops;
- once the 390 candidate is convincing, run `pnpm run check:candidate` and then 360/430;
- deep Android/full E2E stays for PR/release checkpoint according to risk.

## Required visual checkpoint

Before doing broader onboarding work, produce a 390 px screenshot of the rebuilt entry screen.

It should visibly satisfy:

- Friendly Professional + Candy UI + Tactile + Educational;
- teacher-specific visual object(s), not a generic icon;
- strong but not oversized typography;
- intentional density with no giant dead zone;
- tactile depth/pressed language;
- CTA visible without scroll;
- no mascot/owl;
- no generic `icon + title + subtitle + card` template feeling.

If it still looks like a generic onboarding template, rewrite the composition instead of polishing spacing.

## Speed rules

- keep Vite running;
- use HMR;
- do not restart dev server unless necessary;
- do not reread the whole repository;
- do not write long reports during the visual loop;
- do not run 360/430 before 390 is convincing;
- do not create a complete Design System before this screen works;
- extract reusable primitives only after the screen proves the pattern;
- do not touch billing, storage, backup, BNCC or data contracts unless needed for this screen.

## Finish signal

When the first strong candidate exists, stop expansion and report:

`READY FOR DESIGN REVIEW — ONBOARDING ENTRY V2`

Include:

- screenshot 390 px;
- short list of what changed;
- `check:fast` result;
- known visual differences/next fixes.

Do not merge automatically.

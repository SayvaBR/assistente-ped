# Codex Task — Accelerated Mode

Use `docs/WORKING_CONTEXT.md` and `docs/PRODUCTION_SPEED_PROTOCOL.md` as your default context. Do not reread every project document unless the current task needs it.

## Immediate objective

Finish PR #2 correctly and get the project ready for small, fast V2 PRs.

## Run in parallel when the Codex app/worktrees allow it

### Lane A — Baseline evidence

- resolve the management comment on PR #2;
- correct unsupported design-approval claims;
- run fresh tests/build only once at candidate state;
- update final evidence.

### Lane B — Visual preparation

Without changing shared global files, prepare Issue #3 (Splash + Onboarding) by reading the approved design authority, identifying the target structure and listing the minimum files that should live in `src/v2/`.

Do not wait for Lane A to finish before doing this analysis.

### Lane C — QA

- verify no active mascot/owl remains in the runtime UI;
- identify the smallest relevant E2E set for Splash/Onboarding;
- do not run the whole Android pipeline unless the current change requires it.

## Fast loop rules

- keep Vite/HMR running for visual work;
- during iteration use `pnpm run check:fast`;
- use `pnpm run test:changed` or targeted tests when useful;
- do not run full E2E/Android after CSS-only microchanges;
- for UI, render 390px first;
- only generate 360/430 once the 390px candidate is convincing;
- consolidate documentation/evidence at checkpoint, not after every microstep.

## Finish condition for PR #2

- baseline source is complete;
- claims in docs reflect real fresh runs;
- no secrets/data were committed;
- relevant build/tests pass;
- PR body contains final evidence;
- stop before merge.

After review/merge of baseline, create small isolated branches/worktrees for V2 work instead of continuing one giant branch.

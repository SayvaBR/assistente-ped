# Codex Task — Accelerated Multi-Agent Visual Sprint

## Immediate objective

Build a strong `Onboarding Entry V2` and the minimum UI Lab needed to iterate visually fast.

Use subagents. Keep the main chat as orchestrator.

Read first:

1. `docs/WORKING_CONTEXT.md`
2. `docs/PRODUCTION_SPEED_PROTOCOL.md`
3. `docs/MULTI_AGENT_ORCHESTRATION.md`
4. `docs/SCREEN_SPEC_ONBOARDING_ENTRY_V2.md`
5. `docs/UI_LAB_SPEC.md`

Do not reread the whole repository.

## Preserve work

Before editing:

```bash
git status
git branch --show-current
git fetch origin --prune
```

Never use destructive reset. Preserve local work and reconcile safely if remote moved.

## Spawn these agents now

Use the project agents configured in `.codex/config.toml`.

Start in parallel:

1. `visual_director` — read-only review of the rejected onboarding composition and screen spec.
2. `code_mapper` — read-only mapping of the smallest V2 integration path.
3. `ui_worker` — isolated Onboarding Entry V2 implementation, preferably in its own worktree.
4. `ui_lab_worker` — isolated UI Lab MVP, preferably in a second worktree.

Do not start `qa_reviewer` until a convincing 390px candidate exists.

## Ownership

Main/orchestrator owns shared files by default: `package.json`, root router/navigation, global V2 tokens, shared primitives, build config and data/storage/billing contracts.

`ui_worker` owns screen-local onboarding files.

`ui_lab_worker` owns lab/preview/fixture files.

Never allow two workers to edit the same shared file simultaneously.

## Onboarding Entry V2

The current first screen after Splash is rejected as composition. Do not polish it in place.

Rebuild in `src/v2/` following `SCREEN_SPEC_ONBOARDING_ENTRY_V2.md`.

Fast loop:

`edit -> HMR -> 390 render -> screenshot -> visual_director critique -> fix highest-impact difference -> repeat`

During micro-iterations use only `pnpm run check:fast`.

Do not run Android or full E2E for CSS/composition iterations.

## UI Lab

Build the MVP from `docs/UI_LAB_SPEC.md` in parallel.

Minimum goal:

`/__lab -> Onboarding Entry V2 -> 360/390/430 -> default + secondary state -> HMR`

Do not turn it into a framework.

## Candidate gate

When the 390px version is convincing:

1. ask `visual_director` for another critique;
2. apply the single highest-impact remaining correction;
3. start `qa_reviewer`;
4. validate 360/430;
5. run `pnpm run check:candidate`;
6. run only onboarding-related tests/E2E if available.

## Stop condition

Return:

`READY FOR DESIGN REVIEW — ONBOARDING ENTRY V2`

with screenshot 390px, branch/commit/worktree, validation results, visual_director verdict, qa_reviewer findings and UI Lab status.

Do not merge automatically. Do not advance to unrelated screens before review.
